<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.karake.EReport">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconz"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconz"
        android:supportsRtl="true"
        tools:replace="android:icon"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Stock"></activity>
        <activity android:name=".activities.UserProfile" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.SalesDetails" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SelectClient" />
        <activity android:name=".activities.SelectProduct" />
        <activity android:name=".activities.Sales" />
        <activity android:name=".activities.ProductDetails" />
        <activity android:name=".activities.ProductActivity" />
        <activity android:name=".activities.ClientActivity" />
        <activity android:name=".activities.ClientProfile" />
        <activity android:name=".activities.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>